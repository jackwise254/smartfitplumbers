mixin Partner(image)
  .col-auto.col-md-4.col-lg-auto.text-xl-start&attributes(attributes)
    img(src=`${CWD}assets/img/gallery/${image}` alt='Partner')

mixin Partners
  +Section.py-0
    .row.align-items-center.justify-content-xl-between
      h2.text-light.text-center
